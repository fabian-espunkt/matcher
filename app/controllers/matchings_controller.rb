class MatchingsController < ApplicationController
  def index

    # if current_user == "investor"
    #   user_id.name
    #   user_id.position
    #   user_id.company_name
    #   user_id.company_city
    #   user_id.selling_to
    #   user_id.business_models
    #   user_id.sectors

    # else

    # end
  end
  # instanzvariable von allen viewings von current_user
  # die nicht schon ein match sind -- Serviceobjekt (zuständig für matching, verhindert doppeltes Anzeigen)

  # user type abfragen

  # basierend darauf, checken ob 2 viewings matched (checked durch Service Objekt) - wird angezeigt

  # die viewings von current_user,
end
